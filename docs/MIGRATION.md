# Migration from the previous repository state

The previous `main` state is preserved at `archive/pre-kafka-com-20260804`.

The active command-repository branch intentionally removes the former social-system report and static Pages application because they are unrelated to portfolio control. They remain recoverable from the archive branch and are not migrated into the new structure.

Existing automations are not automatically registered as recurring services. Each must be audited for purpose, current target, source freshness, executor, privacy, cost, observed operation, failure behavior, and continued user value before registration.
